{
  "name": "tele-bot",
  "version": "0.3.2",
  "description": "Authentication middleware for Telegraf",
  "description": "Authentication middleware for Telegraf",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf build/ && tsc --project tsconfig.json",
    "dist": "rimraf dist/ && tsc --project tsconfig.dist.json"
  },
  "keywords": [
    "telegram",
    "bot"
  ],
  "author": "@lromeraj",
  "license": "ISC",
  "devDependencies": {
    "@types/node-localstorage": "^1.3.0",
    "rimraf": "^5.0.5",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@types/node": "^20.12.3",
    "node-localstorage": "^2.2.1",
    "telegraf": "^4.16.3"
  }
}
