{
  "name": "tele-bot",
  "version": "0.3.2",
  "description": "A generic Telegram bot",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf build/ && tsc --project tsconfig.json",
    "dist": "rimraf dist/ && tsc --project tsconfig.dist.json"
  },
  "keywords": [
    "telegram"
  ],
  "author": "@lromeraj",
  "license": "ISC",
  "devDependencies": {
    "@types/node-localstorage": "^1.3.0",
    "@types/node-telegram-bot-api": "^0.64.1",
    "rimraf": "^5.0.5",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "node-localstorage": "^2.2.1",
    "node-telegram-bot-api": "^0.64.0"
  }
}
